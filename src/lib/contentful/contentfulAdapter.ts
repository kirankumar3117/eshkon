// Full implementation in Step 4. Stub keeps TypeScript happy during Step 3.
export async function fetchPage(_slug: string, _preview?: boolean): Promise<unknown> {
  throw new Error('Contentful adapter not yet configured. Add CONTENTFUL_* env vars.')
}
